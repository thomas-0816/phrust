# Fastest Engine Known Gaps

Date: 2026-06-28.

This catalog tracks remaining high-value gaps for the v2 fastest-engine work.
It is additive to `docs/performance-known-gaps.md` and
`docs/performance-cranelift-known-gaps.md`.

Generated reports under `target/performance/` are local evidence only and must
not be committed.

Gap wording follows the gate classes in
`docs/performance-optimization-gates.md` (`HARD_BLOCK`, `SUBSET_ALLOWED`,
`EVIDENCE_GATE`). Where a row below says an area is blocked, that means the
broad/generic form: baseline-native and OSR/mid-region resume are
`HARD_BLOCK`; dense object/method/property subsets and by-ref argument
location encoding are `SUBSET_ALLOWED`; exact per-builtin stubs and optimizer
passes are `EVIDENCE_GATE`; process-local compiled-unit caching is
`SUBSET_ALLOWED`.

| Gap ID | Area | Current evidence | Required before closure |
| --- | --- | --- | --- |
| FPE-GAP-REAL-WORKLOAD-REPRESENTATIVENESS | Benchmark corpus | The repo has deterministic smoke and framework-like fixtures plus an acceleration matrix. `docs/performance-fastest-hotpaths.md` now ranks those artifacts but still records them as local counter evidence, not real production workload coverage. | Add broader workload evidence without making wall-clock-only claims. |
| FPE-GAP-PROFILING-EVIDENCE | Measurement | `just fastest-hotpath-report` now builds or reuses the VM, consumes benchmark smoke, framework smoke, acceleration matrix, standalone counter JSON, and optional profiler artifacts, and writes `target/performance/fastest/hotpath-report.{json,md}` plus `docs/performance-fastest-hotpaths.md`. The current generated summary ranks dispatch, strings/output, calls/builtins, allocation, arrays/foreach, properties/methods, native/JIT candidates, and frontend byte scanning. | Keep the report current as new counters and profiler artifacts land; do not close follow-up implementation gaps from this ranking alone. |
| FPE-GAP-SIMD-BYTE-KERNELS | Source/string scanning | `php_source::byte_kernel` now provides safe scalar-first byte search, newline counting, ASCII detection, ASCII identifier chunking, and ASCII case helpers, with `memchr` behind the search facade and parity tests over byte-string inputs. FPE-03 integrated the facade into source-map newline indexing plus lexer inline-HTML, comment, identifier, and constant-string scans. `docs/performance-simd-byte-kernels.md` records the scalar fallback policy and stateful loops intentionally left alone. | Continue replacing clear runtime string/output call sites in focused prompts with parity evidence; keep architecture-specific code behind safe APIs only. |
| FPE-GAP-FRONTEND-BYTE-SCANNING | Lexer/source maps | FPE-03 applies byte-kernel integration to clear frontend delimiter/newline/ASCII loops and preserves lexer, parser, CST, semantic, and frontend CLI validation. `just bench-lexer` remains local advisory evidence only. | Close only after broader source-map/lexer benchmark reports compare representative before/after evidence without relying on wall-clock-only claims. |
| FPE-GAP-DENSE-OBJECT-COVERAGE | Dense bytecode | Dense bytecode covers scalar, direct-call, builtin, array, and by-value foreach shapes. FPE-04 adds explicit unsupported and auto-fallback reason counters plus `bytecode_lowered_by_family` and `bytecode_executed_by_family`; `just bytecode-exec-smoke` now verifies strict unsupported reasons for property read/write, method call, static call, and include, and auto fallback parity for DTO property read/write, method dispatch, static call, magic `__get`, dynamic property, typed property, property hook, and include-path fixtures. Property fetch/assignment, method/static calls, include/eval, exceptions, generators, fibers, and broader object-heavy fixtures still do not execute as dense bytecode because the rich-IR helper paths are embedded in interpreter control-flow with exception, fiber, diagnostics, scope, and IC state. | Add verified dense opcodes only after reusable helper boundaries preserve visibility, magic/hooks, typed/readonly checks, dynamic properties, include path/once behavior, autoload order, exception/fiber propagation, strict unsupported reasons, auto fallback reasons, counters, and focused A/B fixtures. |
| FPE-GAP-SUPERINSTRUCTIONS-V2 | Interpreter dispatch | FPE-05 adds `php-vm dump-bytecode-patterns`, `scripts/performance/superinstruction_patterns.py`, `just superinstruction-patterns`, richer candidate/emitted/executed/skipped/fallback counter maps, and `docs/performance-superinstructions.md`. The current mined strict dense corpus ranks `load_const echo`, `echo load_const`, `store_local discard`, `echo return`, and `load_const load_const` as top pairs. The selected fusion set is enabled by the managed fast runtime where dense bytecode executes and remains `load_const_echo`, `load_local_echo`, and `binary_concat_echo` because those are the only top/current producer-consumer patterns with a simple unfused equivalent and no new helper boundary. | Close additional v2 fusion gaps only after terminator fusion, arithmetic/store chains, call/builtin fusion, array/foreach loop fusion, and dense property fetch have reusable semantic helpers, source-map/debug accounting, fallback/deopt counters, and focused A/B fixtures. |
| FPE-GAP-POLYMORPHIC-CALL-ICS | Calls/builtins | FPE-06 adds capped function-call polymorphic entries for dynamic string callables, megamorphic fallback, VM and CLI smoke coverage, and `builtin_fast_stub_fallback_by_reason` attribution for the existing exact tiny stubs. Static direct calls remain effectively monomorphic because their IR call name is fixed. Generated arginfo metadata exists and is used by validation/reflection infrastructure, but no broader arginfo-derived stubs were added in this scope to avoid bypassing named-argument, by-reference, coercion, TypeError/ValueError, return-type, or reflection behavior without per-builtin differential fixtures. | Additional exact metadata-derived stubs, wider per-builtin generic dispatch attribution, and stdlib differential evidence for each new stub. |
| FPE-GAP-PROPERTY-ASSIGNMENT-ICS | Object writes | FPE-07 adds interpreter-only guarded declared-property assignment ICs with receiver class id, shape/layout epoch, declared property slot, visibility context, typed-property metadata, readonly/init-only state, reference-slot state, hook/magic/dynamic flags, class/property mutation epoch, hit/miss counters, and shape/visibility/type/readonly/hook-magic/reference/dynamic fallback counters. The smoke fixture covers public writes, private/protected in-scope writes, typed valid/invalid writes, dynamic properties, magic `__set`, property hooks, and DTO hydration loops; VM unit coverage separately asserts readonly error fallback classification. | Dense-bytecode property assignment remains an unsupported/auto-fallback family. Executable PHP coverage for reference-slot assignment fallback waits on runtime support for object-property references; the current VM records the counter when such storage is present internally. |
| FPE-GAP-METHOD-CALL-ICS | Method dispatch | FPE-08 adds capped polymorphic method-call IC targets with receiver class id, class/method epoch, method slot, visibility context, static/final/private/override flags, call shape, by-reference compatibility metadata, magic `__call` state, direct dispatch through the existing VM helper, and metadata-only tiny-inline candidate/rejection counters. `inline-cache-smoke`, `perf-flag-matrix`, and `acceleration-matrix` include the method-call guard fixture covering final/normal/polymorphic/override/magic/visibility/named-argument/exception/DTO service loops. | No method bodies are inlined yet. Executable by-reference method-parameter fallback coverage remains blocked by the existing method-runtime unsupported-by-ref-parameter frontend gap. Dense-bytecode method calls remain unsupported/auto-fallback. |
| FPE-GAP-ARRAY-FASTPATHS-V2 | Arrays | Runtime-owned packed metadata now records element summary, key-kind summary, numeric-string key ambiguity, reference presence, COW sharing, mutation epoch, and packed length. Guarded packed fetch/append/by-value foreach paths exist, family/fallback counter maps attribute array fast-path behavior, a guarded packed-int reduction helper rejects non-packed/shared/reference/mixed/overflow shapes, and Cranelift packed-reduction eligibility consumes the runtime metadata. The smoke fixture covers packed all-int arrays, mixed elements, overflow, empty arrays, negative/string/numeric-string keys, references, COW copies, mutation during foreach, nested arrays, and DTO/config traversal. | Full builtin `array_sum`/`min`/`max` reductions, mutation-heavy cases, by-reference foreach, dense array paths beyond proven helpers, and mixed/record-like shapes remain future work with focused differential or PHPT fixtures. |
| FPE-GAP-STRING-OUTPUT-BATCHING | Strings/output | FPE-10 adds adjacent exact-output batching for IR `Echo` and `LoadConst`/`Echo` producer pairs, `output_batched_appends`, `output_batch_bytes`, `concat_prealloc_hits`, `concat_fallback_by_reason`, nested-buffer/binary-string/object/array/resource fallback tests, and the `output_batching_v2.php` smoke fixture. Batching stops before references, object/resource/array/callable/uninitialized conversions, diagnostics, calls, branches, buffer-control builtins, and other PHP-visible boundaries. | Remaining work is executable output-buffer callbacks, broader producer batching beyond proven constant echo pairs, and additional conversion fast paths only with differential fixtures and fallback counters. |
| FPE-GAP-RUNTIME-LAYOUT-COMPACTNESS | Runtime allocation | FPE-11 adds `docs/research/runtime-layout-compactness.md`, request-local counters for value clones, string allocations, array handle clones, COW separations, reference-cell creations, and object allocations, report integration, an exact packed-array construction win, and a VM fixture covering COW, references, object allocation, and shutdown destructor order. | Keep auditing hot clone/allocation sites; close only after broader packed/mixed storage, object-shape, small-string, or arena work lands with destructor/reference/COW proof. |
| FPE-GAP-OPTIMIZER-PROFILE-GUIDANCE | Optimizer | The IR pipeline (constant folding, literal compaction, copy propagation, peephole, branch simplify) now verifier-brackets every mutating pass individually and rolls a failing pass back with a `rolled_back` report instead of failing the compile. Dense bytecode adds a jump-threading pass behind `--dense-jump-threading` (default profile on, baseline off) with trampoline/threaded-edge/rollback counters; app-flow measurement shows 1-4 trampoline blocks per flow reached only by fallthrough, so explicit-edge threading retires ~0 dispatches on the current corpus. Call normalization is covered by the dense direct-call table plus the by-ref location lowering. | Broader DFA/type folding stays gated on evidence; new passes must keep per-pass rollback, A/B switches, parity fixtures, and skip attribution. |
| FPE-GAP-FAST-PRESET-AUDIT | User policy | The shared profile model now makes `default` the normal CLI/server managed fast profile, keeps `baseline` as rollback, accepts `fast` as a compatibility alias, and keeps `experimental-jit` as a developer diagnostics profile. `just default-profile-smoke` compares baseline vs default across selected runtime, stdlib, performance, framework, and local PHPT smoke cases while recording fallback/deopt counters and native availability/execution counters. `just managed-fast-coverage` asserts dense, quickening, superinstruction, inline-cache, array-shape, builtin intrinsic, string/output, include/cache reuse, native policy, and bounded fallback counters. `just fast-preset-smoke` continues to cover the alias. | Broader production workload coverage remains ongoing; low-level compiler diagnostics and bytecode-cache behavior still need explicit evidence before speed claims. |
| FPE-GAP-SELECTIVE-CRANELIFT-REGIONS | Native tier | FPE-14 is closed for the current selective-region gate and the managed runtime now requests the guarded native tier automatically where backend support exists. `docs/performance-selective-cranelift-regions.md`, `docs/performance-cranelift-results.md`, `docs/cranelift-benchmark-methodology.md`, `crates/php_jit/src/eligibility.rs`, Cranelift fixtures, tiering policy, compile-cache keys, side-exit counters, blacklist counters, and guard reports still bound the native tier to narrow exact regions and prohibit generic function JIT expansion. Unsupported platforms record `native_platform_unavailable`; rejected shapes and side exits record exact reasons while interpreter fast paths continue. | Future expansion needs new counter evidence, generic interpreter fallback, side-exit/blacklist accounting, and focused fixtures for each added region. Broader builtins, object/method inlining, conversion-sensitive strings, dynamic calls, references, exceptions, generators, fibers, and OSR remain blocked on deopt/live-state proof. |
| FPE-GAP-BASELINE-NATIVE-PREREQS | Baseline native tier | FPE-15 adds `docs/adr/fast-baseline-native-tier-prerequisites.md`, `php-vm dump-baseline-native-stencil`, `scripts/performance/baseline_native_stencil_smoke.py`, and `just baseline-native-stencil-smoke`. The current stencil report is no-exec only, estimates compile cost/code size/helper/deopt slots over verified dense bytecode, and records unsupported reasons for calls, arrays/COW/key state, and foreach state. | Executable baseline-native code remains blocked until VM-owned executable-memory/W^X policy, code-cache lifecycle, ABI/helper hashes, frame/live-state/deopt metadata, source maps, reference/COW/foreach/exception state, generator/fiber policy, diagnostics/output proof, and PHPT/reference gates are implemented. |
| FPE-GAP-DEOPT-LIVE-STATE | Optimized exits | FPE-16 adds VM-owned report-only deopt metadata in `php_vm::deopt` and `docs/performance-deopt-live-state-osr-metadata.md`. The schema preserves Cranelift side-exit reason codes 1-7, adds VM reasons for call frames, references/COW, foreach, finally, generator/fiber, output-buffer, and unsupported control-flow state, generates verified dense-region metadata for straight-line, branch, loop, by-value foreach, scalar, call, output, and array/dim sites, and rejects try/finally, exception, generator/fiber, by-reference, include/eval, and runtime-gap shapes before any resume guess. | Executable OSR, baseline-native, or broader Cranelift resume remains blocked on exact initialized-value liveness, alias classes, exception/finally/destructor materialization, generator/fiber snapshots, output-buffer callback state, trace invalidation, and PHPT/reference gates. |
| FPE-GAP-COMPARATIVE-FASTEST-MATRIX | External comparison | FPE-17 adds `scripts/performance/fastest_engine_matrix.py`, `just fastest-engine-matrix`, `target/performance/fastest/matrix.{json,md}`, and committed summary `docs/performance-fastest-engine-results.md`. The matrix compares `phrust-baseline-ir`, `phrust-fast-preset`, `phrust-release-fast`, optional PGO and Cranelift rows, `reference-php-cli`, and `reference-php-cli-opcache` when local prerequisites are available. Phrust rows must match baseline output, diagnostics, and exit status before timing is recorded; reference rows surface compatibility gaps explicitly. | Keep extending fixture representativeness and optional production/JIT artifacts before using the matrix for broader speed claims. |
| FPE-GAP-REQUEST-ARENAS | Allocation model | FPE-19 adds `docs/research/request-local-arenas.md` and explicit request-arena/persistent-engine counters: `request_arena_allocations`, `request_arena_bytes`, `request_pool_resets`, `persistent_engine_allocations`, `persistent_engine_bytes`, `arena_fallback_allocations_by_reason`, and `destructor_sensitive_arena_blocks`. The current narrow win is request-local frame/register reuse for eligible plain calls; broad arena reset remains disabled for objects, arrays, references, resources, generators/fibers, output-buffer callbacks, try/finally, and any destructor-sensitive storage. | Close only after an owned request arena proves teardown equivalence for each allocation class, persistent engine data has strict immutable ownership/invalidation, and userland object/value/resource state is never preserved across requests. |
| FPE-GAP-PERSISTENT-FEEDBACK | Cross-request metadata | FPE-20 adds `docs/research/persistent-type-feedback.md`, a metadata-only feedback model in `php_vm::persistent_feedback`, CLI advisory loading with `--persistent-feedback-read`, stats output with `--persistent-feedback-stats-json`, stale/corrupt/userland-state rejection, and optional/default-off matrix rows. Accepted entries are still advisory and do not alter execution. | Close only after an owned engine cache writer persists accepted metadata, class/function/autoload/include epochs are sourced from persistent engine state, classmap fingerprints are modeled where applicable, and accepted feedback is consumed by quickening/IC/tier policy under A/B correctness gates. |
| FPE-GAP-NUMERIC-STRING-SPECIALIZATION | PHP conversions | FPE-21 documents the numeric-string model in `docs/performance-numeric-string-cache.md`, extends `php_runtime::numeric_string` with canonical integer/float and overflow/precision-sensitive metadata, routes array-key normalization through the shared classifier, exposes the cache to arithmetic, loose comparison, array-key conversion, and modeled builtins, and reports `numeric_string_classify_calls`, cache hits/misses, specialization hits, warning-sensitive fallbacks, and overflow/precision fallbacks. The VM has a guarded integer/numeric-string arithmetic specialization for safe add/subtract/multiply and falls back to shared scalar helpers for leading, float, overflow, precision-sensitive, division, modulo, and diagnostic-sensitive cases. | Persistent quickening opcodes for numeric-string compare and array-key lookup remain future work, and broader PHPT numeric-string modules should keep expanding the fixture matrix before promoting any default-on policy. |
| FPE-GAP-REFERENCE-ALIAS-DEOPT | References | FPE-22 adds `php_vm::AliasState` and documents the deoptimization policy in `docs/performance-reference-aliasing-deopt.md`. VM counters now report `frame_alias_state`, `alias_state_transitions`, `fast_path_disabled_by_reference`, `dequickened_by_reference`, `IC_invalidated_by_reference`, and `dense_bytecode_fallback_by_reference`. Local `=&`, globals, by-reference params/returns/captures, array-dim references, foreach-by-reference, property reference-slot IC exits, dense bytecode reference fallback, and native eligibility rejection are classified conservatively. Fixtures cover no-reference hot paths, local refs, by-ref params/returns, array element refs, globals, unset/rebinding, foreach-by-ref, closure escape, and the existing object-property reference diagnostic gap. | Optimizing through references remains disabled. Packed-array fast paths over reference/COW storage, property assignment IC reference slots, call-frame reuse for by-ref params, dense bytecode reference/COW/by-ref states, and native/JIT by-ref/reference-producing shapes all stay generic or rejected until exact alias identity and write-through behavior have broader differential and PHPT proof. |
| FPE-GAP-RECORD-LIKE-ARRAY-SHAPES | Mixed arrays | FPE-23 adds runtime-owned array shape metadata for empty, packed, packed-with-holes, small inline map, interned-string-key record, shape-stable record-like, mixed hash, shared immutable literal, and COW/reference fallback arrays. VM `FetchDim` and one-dimensional `IssetDim` now use fail-closed helper reads for record-like and small-map shapes, and counters report observed shapes, record/small-map hits and misses, key-coercion fallbacks, order-semantics fallbacks, and COW/reference fallbacks. `docs/performance-array-shapes.md` documents the policy and covered fixtures. | Representation changes, shape interning, immutable literal storage, dense bytecode/native consumption, and promotion beyond guarded helper reads still require broader framework/PHPT evidence plus exact mutation, insertion-order, numeric-string-key, COW, reference, and foreach invalidation proof. |
| FPE-GAP-INCLUDE-AUTOLOAD-GRAPH | Include/autoload | FPE-24 adds `docs/research/include-autoload-dependency-graph.md`, graph counters for include/autoload hits and misses, side-effect-safe negative autoload lookup hit accounting, file-fingerprint invalidation attribution, and path-semantics fallback attribution. Existing include/autoload IC fixtures now cover include/once order, include_path order, missing-path fallback, classmap-style autoload, side-effectful missing autoloads, negative lookup hits, autoload epoch invalidation, and file mutation invalidation. | Directory-versioned negative include-path caching, function/constant lookup graph nodes, portable inode/content fingerprints, classmap fingerprints, production-mode deployment fingerprints, and persistent-feedback integration remain future work. |
| FPE-GAP-BUILTIN-INTRINSICS | Builtins | FPE-25 adds `docs/performance-builtin-intrinsics.md`, explicit intrinsic counters, and exact interpreter intrinsic stubs for `strtolower`, `str_contains`, `str_starts_with`, and `str_ends_with` beyond the FPE-06 `strlen`, `count`, `is_int`, `is_string`, and `is_array` set. The stdlib differential fixture `STDLIB_BUILTIN_INTRINSICS` covers wrong arity, wrong type, named arguments, reference-backed variables, catchable errors, binary strings, large inputs, and edge empty strings. | Specialized bytecode/native builtin forms, arginfo-derived wider stubs, array/search/string allocation intrinsics, JSON/HTML/filesystem stateful intrinsics, and persistent feedback integration remain future work. |
| FPE-GAP-SPECIALIZED-CALL-FRAMES | Calls | `docs/performance-specialized-call-frames.md` defines VM-owned frame layout classes and the narrow executable fast path. The VM now reports `call_frame_layout_observed`, `tiny_frame_candidates`, `specialized_frame_hits`, `generic_frame_fallback_by_reason`, `arg_array_avoided`, and `heap_frame_avoided`; exact tiny leaf user functions can avoid cloning the active argument snapshot after normal argument preparation, while methods, closures, named/variadic calls, by-reference shapes, generators, fibers, include/eval, and non-leaf functions stay on the generic frame path with fallback attribution. | Wider direct argument passing for methods/closures/variadics, dedicated heap-frame layouts beyond the request-local frame pool, dense/native consumption, reflection-specific executable coverage, and broader PHPT/framework proof remain future work. |
| FPE-GAP-HOT-COLD-SPLITTING | Code layout | `docs/performance-hot-cold-splitting.md` audits arithmetic, concat, compare, arrays, property fetch/assign, calls/builtins, output, and include/autoload boundaries. The VM now reports aggregate `slow_path_calls_by_reason` entries while retaining detailed family maps, and only already-isolated fallback classifiers/diagnostic helpers are marked cold. Existing warning/error-order tests now assert the aggregate slow-path reasons for output, concat, and include/require fallbacks. | Broader handler outlining, platform-stable instruction-count evidence, and any new fast-path split must keep shared semantic helpers plus focused warning/reference/magic/COW/destructor tests. |
| FPE-GAP-REGION-PROFILING | Future compilers | FPE-28 adds `php-vm run --region-profile-json <path>` and `PHRUST_REGION_PROFILE_JSON`, plus `docs/performance-region-profiling.md`. Reports are metadata-only, file-output only, privacy constrained, and classify bounded framework-like regions for router dispatch, middleware/service calls, container lookup, template render, JSON response, DTO/ORM hydration, and array/config traversal. `just framework-smoke` now records per-run region profile paths and summaries under `target/performance/framework-smoke/`. | Executable region compilation remains blocked on taken/not-taken branch feedback, exact live-state/deopt maps, exception/finally/destructor materialization, generator/fiber snapshots, trace invalidation, and PHPT/reference proof. |
| FPE-GAP-COPY-PATCH-STENCILS | Baseline native research | `docs/research/copy-and-patch-stencil-tier.md` and `php-vm dump-copy-patch-stencils <file.php> --json` now define a no-exec textual stencil format over quickening-compatible dense bytecode. `just copy-patch-stencil-smoke` writes reports under `target/performance/stencils/` with code-size, patch-site, helper-call, unsupported-reason, deopt-point, live-state, and work-to-compile estimates. | Executable copy-and-patch remains blocked on VM-owned executable memory/W^X policy, code-cache lifecycle, helper/ABI hashes, exact live-state/deopt maps, invalidation epochs, reference/COW/foreach/exception state, property dense opcodes, and PHPT/reference proof. |
| FPE-GAP-PHP-MID-TIER | Mid-tier research | `docs/research/php-mid-tier-compiler.md` defines the PHP-semantics-aware mid-tier, its inputs, candidate optimizations, strict rejection conditions, placement against interpreter/stencils/Cranelift, and prerequisite list. `php-vm dump-mid-tier-plan <file.php> --json` and `just mid-tier-plan-smoke` now classify dense functions/regions as metadata-only eligible or ineligible and report expected guards, helpers, deopt points, and rejection reasons under `target/performance/mid-tier/`. | Executable mid-tier compilation remains blocked on property/method dense metadata, exact IC/shape/numeric-string feedback, alias/reference/COW summaries, branch-bias history, persistent feedback writing, full deopt/live-state materialization, helper ABI/status hashes, invalidation epochs, and PHPT/reference proof. |

## Closure Policy

A fastest-engine gap is closed only when the owning work lands its required
source changes or docs, focused fixtures, counters or reports, off switches,
fallback paths, validation evidence, and skip classification for optional tools.
Wall-clock-only evidence cannot close a gap.
