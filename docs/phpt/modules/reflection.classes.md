# reflection.classes PHPT coverage

## Verified scope

- `ReflectionClass` names, namespaces, extension owners, flags, parents,
  interfaces, and member lists selected by the module manifest.

## Known gaps

- Full class metadata parity for traits, anonymous classes, dynamic members,
  constants, doc comments, and source locations remains future work.
