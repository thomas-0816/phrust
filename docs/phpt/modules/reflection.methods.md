# reflection.methods PHPT coverage

## Verified scope

- `ReflectionMethod` name, declaring class, modifiers, parameters, and return
  types selected by the module manifest.

## Known gaps

- Full method prototype, trait-alias, abstract/final/private edge cases, and
  exact exception text remain outside the selected fixture.
