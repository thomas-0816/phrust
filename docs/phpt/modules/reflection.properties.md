# reflection.properties PHPT coverage

## Verified scope

- `ReflectionProperty` name, declaring class, visibility, static, readonly,
  modifier bits, and type metadata selected by the module manifest.

## Known gaps

- Full property default, initialization, dynamic property, attribute, hook, and
  exact exception parity remains outside the selected fixtures.
