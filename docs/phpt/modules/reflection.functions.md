# reflection.functions PHPT coverage

## Verified scope

- `ReflectionFunction` metadata for selected internal and userland functions.

## Known gaps

- Full closure, callable, static-variable, doc-comment, source-location, and
  default-value parity remains future selected-fixture work.
