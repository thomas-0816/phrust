# reflection.enums PHPT coverage

## Verified scope

- `ReflectionEnum`, `ReflectionEnumUnitCase`, and
  `ReflectionEnumBackedCase` metadata.
- Backed flags, backing type, and case-existence behavior selected by the
  module manifest.

## Known gaps

- Full enum reflection parity, exact exception text, attributes on cases, and
  inherited interface edge cases remain outside the selected fixtures.
