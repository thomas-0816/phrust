# reflection.attributes PHPT coverage

## Verified scope

- `ReflectionAttribute` names, arguments, repeat metadata, and attached
  targets selected by the module manifest.

## Known gaps

- Full attribute instantiation, target validation, repeatability diagnostics,
  and namespace edge cases remain outside the selected fixture.
