# Standard library Known Gaps

Reference target: PHP 8.5.7 (`php-8.5.7`).

Standard library known gaps are tracked per test ID. A gap is valid only when the test,
reference expectation, reason, and next-layer priority are listed here or in a
generated Standard library coverage report.

Registry drift between runtime builtin registration and `php_std` metadata is
checked by `nix develop -c just stdlib-registry-drift`. Temporary symbol-level
drift belongs in `scripts/stdlib/registry_drift_allowlist.jsonl`; generated
reports under `target/stdlib/registry-drift/` are local evidence and are not
committed.

| Test ID | Area | Reason | Priority |
| --- | --- | --- | --- |
| STDLIB-GAP-PHAR-REQUIRED | PHAR | The optional read-only PHAR MVP covers local uncompressed archive reads, `phar://` file access, class visibility, and selected static capability probes. Writable archives, `PharData`, compressed archive reads/writes, bz2 support, signature validation/enforcement, metadata APIs, and full stub/diagnostic parity remain outside required Standard library gates. | Performance |
| STDLIB-GAP-EXTENSION-PHPT-PROMOTION | Extension PHPT smoke | The standard library selects upstream PHPTs from `ext/standard`, `spl`, `json`, `pcre`, and `date` without vendoring the suite. Only currently stable cases are promoted to `run`; broader upstream extension PHPTs remain explicit skips or known gaps until their local differential fixtures and byte-parity gaps are closed. | Later Standard library/7 |
| STDLIB-GAP-FULL-PARITY | Standard library coverage | The standard library generates deterministic function/class/constant coverage against `REFERENCE_PHP` when available. The report is allowed to mark reference-only standard-library symbols as known gaps until their local fixtures, runtime builtins, and byte-parity behavior are implemented. | Later Standard library/7 |
| STDLIB-GAP-COMPOSER-SOURCE-CHECKOUT | Composer source mode | The standard library provides an opt-in `PHRUST_STDLIB_COMPOSER_SOURCE_DIR` smoke for a local Composer source checkout and skips cleanly when absent. A vendored Composer source tree, online Composer workflows, plugin/script execution, and full console bootstrap parity are not required Standard library artifacts. | Performance |
| STDLIB-GAP-NETWORK-STREAMS | Streams | Remote URLs, TLS, curl, and openssl are out of required scope and default-off. | Performance+ |
| STDLIB-GAP-MBSTRING-INTL | Extensions | Full `mbstring`, `intl`, and ICU semantics are out of required scope. `mbstring` has a bounded UTF-8 MVP for selected Composer/platform checks; legacy encodings, mbregex, Oniguruma, locale-sensitive behavior beyond selected fixtures, and broad upstream parity remain gaps. | Performance+ |
| STDLIB-GAP-DOM-XML-PDO-SESSIONS | Extensions | DOM/XML/SimpleXML, PDO, and sessions are not required Standard library extension surfaces. | Performance+ |
| STDLIB-GAP-PROCESS-CAPABILITY | Process | The standard library registers the process/shell API surface and returns deterministic PHP-visible failures by default; isolated tests can use a mock for `shell_exec`, `exec`, `system`, and `passthru`. Real process execution, pipe resources, and full `proc_*`/`popen` resource semantics remain out of Standard library. | Performance |
| STDLIB-GAP-ZEND-ABI-SAPI | Runtime | Zend C extension ABI, FPM, Apache SAPI, and CGI production behavior are non-goals. | Later |
| STDLIB-GAP-STREAM-PRODUCERS | Streams | The streams work implements the resource value model, `ResourceTable`, local file/php wrapper registry, root-constrained file handle operations, deterministic file read/write helpers, directory resources, stream metadata/content/copy helpers, stream contexts, and include-path resolution. Network streams, stream filters, and byte-perfect stream metadata remain outside this MVP. | Later Standard library streams/filesystem work items |
| STDLIB-GAP-DEBUG-BYTE-PERFECT-ZVALS | Debug output | The standard library implements recursion-safe Composer/debug-compatible `print_r`, `var_dump`, and `var_export` MVPs, but not byte-perfect Zend zval metadata, resource formatting, or every object visibility marker. | Later Standard library/7 |
| STDLIB-GAP-SERIALIZE-REFERENCES | Serialization | The standard library serializes runtime `ReferenceCell` values by effective value and guards cycles, but does not emit or parse PHP `R`/`r` reference records yet. | Later Standard library |
| STDLIB-GAP-UNSERIALIZE-ALLOWED-CLASSES | Serialization | `unserialize` accepts an options argument for arity compatibility but does not enforce `allowed_classes`; object payloads are restored as simple runtime objects. | Later Standard library |
| STDLIB-GAP-SERIALIZE-MAGIC-RESOURCE | Serialization | `__serialize`, `__unserialize`, legacy Serializable hooks, incomplete classes, and resources are outside the 06.11 MVP. | Later Standard library/7 |
| STDLIB-GAP-STRING-LOCALE-SPECIFIC | String functions | The string case and word functions are deterministic ASCII/C-locale MVPs; host-locale-sensitive behavior is intentionally not modeled. | Later locale/intl work |
| STDLIB-GAP-HTML-FULL-ENTITIES-FLAGS | HTML string helpers | `htmlspecialchars` covers common default escaping and `htmlentities` covers the core special characters plus the HTML4 Latin-1 entity set and `&euro;` for UTF-8 output parity in selected extension fixtures. Full generated entity tables, complete charset handling, and exhaustive non-default flag combinations are not implemented. | Later HTML/entity compatibility |
| STDLIB-GAP-HTTP-BUILD-QUERY-OPTIONS | URL/query helpers | `http_build_query` supports deterministic array-to-query MVP output, but `numeric_prefix`, custom separators, RFC mode switches, objects, and byte-perfect nested edge cases are not implemented. | Later URL/query compatibility |
| STDLIB-GAP-HASH-RANDOM-ALGORITHMS | Hash/random helpers | Optional hash/random support covers selected digest, HMAC, file, streaming context, equality, and legacy mhash compatibility helpers plus OS-backed random shape/range behavior. Remaining gaps include HashContext serialization parity, full xxHash secret object/stringable diagnostics, file-fixture path edge cases, and every error-message byte. | Later hash/random compatibility |
| STDLIB-GAP-PRINTF-ADVANCED-FORMATS | Formatted output | The standard library implements Composer-typical `%s`, `%d`, `%u`, `%f`, `%F`, `%x`, `%X`, `%o`, `%c`, `%%`, width, precision, and padding, but not positional arguments, locale-specific formatting, scientific/general specifiers, or byte-perfect PHP edge cases. | Later formatting compatibility |
| STDLIB-GAP-FPRINTF-STREAM-RESOURCE | Formatted output / streams | `fwrite` now covers writable PHP-visible streams, but `fprintf` is still registered as deterministic failure until formatted output is wired to `ResourceRef` writes. | Later formatted stream compatibility |
| STDLIB-GAP-STAT-BYTE-PERFECT | Filesystem stat | The standard library implements root-constrained path/stat helpers and conditional symlink coverage, but `stat`/`lstat` expose stable MVP fields instead of every PHP numeric and associative stat slot and byte-perfect platform mode values. | Later filesystem compatibility |
| STDLIB-GAP-FILE-WARNING-TEXT | Filesystem file I/O | The file operations return deterministic PHP-style failure values for invalid modes, missing handles, and denied paths, but do not yet emit byte-perfect PHP warning text for every failure. | Later diagnostics compatibility |
| STDLIB-GAP-TMPFILE-LIFETIME | Filesystem file I/O | `tmpfile` creates a deterministic file under the first allowed root and returns a stream resource, but does not yet implement PHP's automatic unlink-on-close lifetime. | Later filesystem compatibility |
| STDLIB-GAP-GLOB-ADVANCED | Filesystem glob | `glob` supports deterministic local single-directory `*` and `?` patterns, but not recursive patterns, brace expansion, character classes, flags, or platform-specific shell glob details. | Later filesystem compatibility |
| STDLIB-GAP-STREAM-METADATA-BYTE-PERFECT | Streams | `stream_get_meta_data` exposes stable Composer-facing MVP fields, but not every PHP metadata key or wrapper-specific value. | Later stream compatibility |
| STDLIB-GAP-STREAM-TTY-HOST-PROBE | Streams | `stream_isatty` returns deterministic `false`; host TTY probing is not enabled by default capabilities. | Later capability-gated stream compatibility |
| STDLIB-GAP-JSON-FLAGS-BYTE-PERFECT | JSON | The standard library implements strict JSON parsing/encoding and common flags, but not every PHP flag interaction, bigint string preservation, UTF-8 ignore/substitute behavior, or byte-perfect float formatting. | Later JSON compatibility |
| STDLIB-GAP-JSONSERIALIZABLE-DISPATCH | JSON | `JsonSerializable` is registered as a PHP-visible interface symbol, but `json_encode` does not yet invoke userland `jsonSerialize()` methods. Concrete example: `class C implements JsonSerializable { public function jsonSerialize(): mixed { return ["x" => 1]; } } echo json_encode(new C());` should encode the returned array, but the runtime builtin layer currently has no clean bridge into VM method dispatch. | Later object/call dispatch integration |
| STDLIB-GAP-PCRE-ADVANCED-FLAGS | PCRE | This implementation uses PCRE2 and covers common delimiters, malformed-pattern warnings, PHP-style PCRE2 compile warnings, replacement-family compile-error null returns and pattern/replacement type checks, named captures, PCRE `(*MARK:...)` match-array entries, named duplicate capture precedence, trailing unmatched capture trimming, offsets, `PHP_INT_MIN` offset `ValueError`, `/u` codepoint-boundary offset errors, `/u` matching after invalid UTF-8 prefixes when the requested offset starts a valid suffix, `/u` malformed UTF-8 subject diagnostics, request-local `pcre.backtrack_limit`, `pcre.recursion_limit`, and PHP-sized JIT stack match failures, `preg_match`/`preg_match_all` flag validation, pattern argument `TypeError` parity, non-variable `preg_match_all` matches by-reference fatal shape, UTF-8 split/match iteration with selected `htmlentities` entity rendering, `preg_replace` malformed-character-class compile-error parity, `preg_replace_callback` array-pattern dispatch and nested-array subject string-cast warnings, `preg_replace_callback_array` callback-map errors, `i/m/s/x/u` plus selected `A/D/J/n/r/U/S` modifiers and whitespace modifier tails, replacements, split including zero-width delimiter and `\K` bump-along handling, grep including array element string-cast warnings without input mutation, quote behavior including `preg_quote` NUL-byte escaping, callback dispatch, callback-array dispatch, filter behavior, PCRE version constants, `PCRE_JIT_SUPPORT`, remaining PCRE constants, request-shutdown safety when user stream wrapper `stream_close()` callbacks call `preg_*`, and request-local last-error state in the focused PHPT harness. Full upstream parity is currently 152 PASS, 7 SKIP, and 6 FAIL from 165 upstream `ext/pcre` rows; remaining gaps include callout surface, callback/trampoline edge cases, locale-sensitive behavior, and remaining byte-perfect warning/path/stack text. | Later PCRE compatibility |
| STDLIB-GAP-PCRE-CALLBACK-DISPATCH | PCRE | `preg_replace_callback` and `preg_replace_callback_array` dispatch named functions and closures through the VM callable path in the selected fixtures. Direct runtime-registry callback use remains limited to internal callables, and array/method callback edge cases remain outside this scope. | Later VM callable integration |
| STDLIB-GAP-DATE-TIMELIB-PARITY | Date/Time | The Date/Time MVP uses a Rust-owned abstraction with a deterministic timezone registry, ISO-like absolute parsing, restricted relative modifiers, timestamp-like `strtotime` input, simple day-relative modifiers, and common timestamp formatting. Full timelib natural-language parsing, aliases, abbreviation resolution, numeric-offset parsing parity, and warning/exception byte parity are not implemented yet. | Later Date/Time compatibility |
| STDLIB-GAP-DATETIME-FULL-API | Date/Time | `DateTime`, `DateTimeImmutable`, `DateTimeZone`, and `DateInterval` now have internal runtime construction and selected VM method dispatch for formatting, timestamps, timezone access/mutation, interval add/sub, modify, diff, names, and interval formatting including upstream padded interval fields. Full DatePeriod support, `createFromFormat`, complete class/interface metadata, exhaustive method/property surfaces, and byte-perfect diagnostic parity remain outside the MVP. | Later Date/Time compatibility |
| STDLIB-GAP-DATETIME-TZDB-DST | Date/Time | Timezone offsets are deterministic fixed-offset MVP values for the supported identifier registry; tzdb historical transitions, daylight-saving changes, leap seconds, and locale-sensitive timezone names are not modeled. | Later Date/Time compatibility |
| STDLIB-GAP-SPL-INTERFACE-METHOD-SURFACES | SPL | The SPL work registers SPL basis interfaces and implement the iterator methods needed by the internal iterator MVP, but they do not yet enforce every SPL interface method signature or implement full ArrayAccess/Serializable behavior. | Later SPL compatibility |
| STDLIB-GAP-SPL-AUTOLOAD-ADVANCED | SPL | VM autoload stack registration, unregister, listing, direct call, and ordered callback invocation are implemented for Composer-style string/closure/user-function callbacks. Prepend behavior, throw flag exactness, default `spl_autoload`, namespace/path conventions, and exact warning text remain outside this scope. | Later SPL/autoload compatibility |
| STDLIB-GAP-SPL-OBJECT-HASH-PARITY | SPL | `spl_object_id` and `spl_object_hash` are stable for runtime object identity, but the hash is a deterministic engine-local hex rendering rather than a byte-for-byte Zend handle hash guarantee. | Later SPL object parity |
| STDLIB-GAP-SPL-ITERATOR-MUTATION-EDGES | SPL | The standard library snapshots iterator sources for deterministic foreach and method behavior. Live mutation during iteration, by-reference iterator values, seek side effects, nested iterator invalidation, and PHP's exact object/array aliasing edge cases are not modeled. | Later SPL iterator compatibility |
| STDLIB-GAP-SPL-ITERATOR-FULL-API | SPL | The standard library implements the core methods needed for `ArrayIterator`, `RecursiveArrayIterator`, `IteratorIterator`, `LimitIterator`, `EmptyIterator`, `AppendIterator`, and selected `CachingIterator` foreach/full-cache interop, but not the complete SPL method surface such as flags, seek, recursive-child APIs, sub-iterator controls, serialization, catchable method exceptions, or byte-perfect exception/warning text. | Later SPL iterator compatibility |
| STDLIB-GAP-SPL-CONTAINER-FULL-API | SPL | The standard library implements `ArrayObject`, `SplFixedArray`, `SplObjectStorage`, `SplDoublyLinkedList`, `SplStack`, and `SplQueue` MVP storage, Countable, foreach, and common method behavior, but not complete flags, iterator modes, serialization, cloning, recursive object semantics, exact exception/warning classes, or every SPL method. | Later SPL container compatibility |
| STDLIB-GAP-SPL-CONTAINER-NESTED-ARRAYACCESS | SPL | The standard library supports one-dimensional `ArrayObject`/`SplFixedArray`/SPL iterator ArrayAccess syntax for covered classes, one-dimensional userland `ArrayAccess` offset read/write/append/isset/empty/unset dispatch, and method-based `SplObjectStorage` object keys. Nested ArrayAccess writes, by-reference offsets, catchable SPL ArrayAccess method exceptions, and object-key bracket syntax for `SplObjectStorage` need a raw-key lvalue model beyond the Standard library MVP. | Later VM lvalue/ArrayAccess compatibility |
| STDLIB-GAP-SPL-FILE-FULL-API | SPL file classes | The standard library implements root-constrained local `SplFileInfo`, `SplFileObject`, and empty `SplTempFileObject` MVPs for common path/stat/read/foreach methods, but not full seek modes, file flags, permission predicates, writable temp streams, locking, chmod/chown, openFile variants, or byte-perfect exceptions/warnings. | Later SPL file compatibility |
| STDLIB-GAP-SPL-FILE-CSV-FLAGS | SPL file classes | `SplFileObject::fgetcsv()` supports a simple delimiter MVP over the next line. Enclosure/escape controls, CSV control state, multiline quoted fields, flag interactions, locale edge cases, and PHP warning parity remain outside this MVP. | Later SPL CSV compatibility |
| STDLIB-GAP-REFLECTION-ARGINfo-PARITY | Reflection | The standard library exposes internal functions through `ReflectionFunction` and `ReflectionParameter`, including deterministic smoke signatures for common builtins. The Standard library registry does not yet carry generated PHP 8.5 arginfo for every internal symbol, so full parameter names, union/intersection types, by-reference/variadic flags, default constants, and exact optionality remain incomplete. | Later generated arginfo import |
| STDLIB-GAP-REFLECTION-INTERNAL-METHOD-SURFACE | Reflection | The runtime reflects internal classes and selected common methods for extension/name/location smoke coverage. It does not enumerate every method, property, class constant, modifier flag, inheritance edge, or byte-perfect exception path for every internal class. | Later internal class metadata expansion |
| STDLIB-GAP-REFLECTION-EXTENSION-METADATA | Reflection | `ReflectionExtension` lists enabled registry functions and classes, but extension versions, dependencies, INI entries, constants as reflection objects, persistent/module state, and exact PHP output formatting are not modeled. | Later extension metadata expansion |
| STDLIB-GAP-REFLECTION-ATTRIBUTE-NEWINSTANCE | Reflection attributes | The standard library exposes reflected attribute names, constant-expression arguments, repeat metadata, and bounded userland `ReflectionAttribute::newInstance()` construction through normal constructor calls. Full parity still needs target validation, repeatability rules, named argument details, internal attributes, exact diagnostics, and autoload-sensitive class resolution. | Later reflection constructor integration |
| STDLIB-GAP-REFLECTION-PROPERTY-HOOK-PARITY | Reflection properties | The standard library exposes whether lowered property hooks exist and which hook names are present. Full PHP 8.5 hook reflection still needs hook `ReflectionMethod` objects, exact virtual/backed semantics, raw-value accessors, asymmetric visibility edge cases, and byte-perfect diagnostics. | Later property hook reflection parity |
| STDLIB-GAP-TOKENIZER-TOKEN-PARSE-CONTEXT | Tokenizer | `TOKEN_PARSE` is accepted only for cases the current `php_lexer` already tokenizes the same way with or without the flag. PHP parse-error throwing and contextual reserved-word retokenization remain gaps and are excluded from the tokenizer PHPT dev gate. | Later lexer/tokenizer parity |
| STDLIB-GAP-TOKENIZER-ZEND-NUMERIC-IDS | Tokenizer | The standard library uses a versioned Lexer local token ID table shared by `token_get_all`, `token_name`, token constants, and `PhpToken::is`. Numeric IDs are internally stable for PHP 8.5.7 in this engine, but are not byte-for-byte Zend token constant values. | Later Zend token ID import if required |
| STDLIB-GAP-ARRAY-MERGE-RECURSIVE-REFERENCES | Array functions | `array_merge_recursive` implements deterministic scalar/array merge behavior for Composer-style arrays, but not every Zend reference, object, and recursive-array edge case. | Later array compatibility |
| STDLIB-GAP-ARRAY-WALK-BY-REF-MUTATION | Array callback functions | The `array_walk` implementation invokes callbacks with value/key/userdata arguments and propagates errors, but it does not yet pass element references into callbacks that declare `&$value`. | Later array reference compatibility |
| STDLIB-GAP-NATURAL-SORT-EDGE-CASES | Array sorting | The standard library implements deterministic natural ordering for common ASCII filenames, but not every Zend leading-zero, locale-sensitive, or byte-perfect `strnatcmp` edge case. | Later natural-sort compatibility |
| STDLIB-GAP-MATH-FLOAT-EDGES | Math/numeric functions | The standard library implements common integer, float, numeric-string, NaN/INF, and formatting paths, but not every `PHP_ROUND_*` mode, negative-zero formatting detail, overflow shape, or architecture-sensitive float edge case. | Later math compatibility |
| STDLIB-GAP-FORWARD-STATIC-CALL-EDGE-PARITY | Function handling | The standard library routes `forward_static_call` through the VM callable path for ordinary static array/string callables inside class scope, but exact Zend forwarding semantics for complex late-static-binding chains, visibility edge cases, and diagnostic text are not byte-perfect. | Later callable/static-scope parity |
| STDLIB-GAP-CALL-USER-FUNC-ARRAY-BYREF | Function handling | The standard library supports positional and named array arguments for `call_user_func_array`, but array elements are not yet promoted into by-reference call slots for callbacks that require `&$param`. | Later reference-call compatibility |
| STDLIB-GAP-OUTPUT-BUFFER-CALLBACKS | Output buffering | The standard library implements the output buffer stack and core clean/flush/read operations, but not `ob_start` callback transformation, chunk flags, or callback lifecycle edge cases. | Later output-buffer compatibility |

work item-specific gaps must be appended with concrete fixture IDs as Standard library
implementation proceeds.
